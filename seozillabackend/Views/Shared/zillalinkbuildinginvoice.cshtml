@model seozillabackend.Models.order

<div class="row">
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Quantity</th>
                    <th>Details</th>
                    <th>Unit Cost</th>
                    <th>Total</th>

                </tr>
            </thead>
            <tbody>

                @foreach (var itemzillalinkbuilding in Model.zillalinkbuildings.Take(1))
                {

                    <tr>
                        <td class="count">@ViewBag.countzillalinkbuilding</td>
                        <td class="details"> @itemzillalinkbuilding.plan @ViewBag.service</td>

                        <td>£<span class="unit-cost"></span></td>
                        <td>£<span class="total-cost"></span></td>

                    </tr>
                }

            </tbody>
        </table>


    </div>

</div>


